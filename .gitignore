# Ignore input and output directories
sample_input/
sample_output/
output_run/

# Test files (excluded from sharing for runtime-only distribution)
tests/
.pytest_cache/

# Python caching & Virtual environment
.venv/
venv/
__pycache__/
*.pyc

# OS files
.DS_Store

# Log data inputs
input_csv/

# Configuration files (only templates are shared via Git, active copies are local-only)
config/column_config.json
config/system_commands.json
config/target_categories.json
config/common_config.json
s3_fetcher/s3_config.json
